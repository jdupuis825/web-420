#-----------------------------------------
# Title: dupuis-sessions.yaml
# Author: Jocelyn Dupuis
# Date: 09/7/2023
# Description: File for sessions API
#-----------------------------------------

# OpenAPI version 
openapi: 3.0.0
info:
  title: Persons API
  version: 1.0.0

# Tags 
tags:
  - name: People

# API Paths
paths:
  /api/people:
  # GET operation 
    get:
      tags:
        - People
      summary: Retrieve all People
      responses:
        '200':
          description: Successful operation
        '500':
          description: Server Exception
        '501':
          description: MongoDB Exception
    # POST operation 
    post:
      tags:
        - People
      summary: Creates a person
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                lastName:
                  type: string
                dependents:
                  type: array
                  items:
                    type: string
                roles:
                  type: array
                  items:
                    type: string
                birthDate:
                  type: string
                
      responses:
        '200':
          description: Successful operation
        '500':
          description: Server Exception
        '501':
          description: MongoDB Exception